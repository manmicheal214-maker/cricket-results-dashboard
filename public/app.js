const state = { matches: [], filter: "completed", query: "", competition: "all" };
const $ = (selector) => document.querySelector(selector);
async function load(){const message=$("#message"),container=$("#matches"),empty=$("#empty");message.textContent="Loading matches…";container.innerHTML="";empty.classList.add("hidden");const controller=new AbortController();const timeout=setTimeout(()=>controller.abort(),10000);try{const response=await fetch(new URL("data/matches.json?t="+Date.now(),document.baseURI).href,{cache:"no-store",signal:controller.signal});clearTimeout(timeout);if(!response.ok)throw new Error(`Data request failed (${response.status})`);const text=await response.text();if(text.trim().startsWith("<"))throw new Error("The data URL returned HTML instead of JSON");const payload=JSON.parse(text);state.matches=Array.isArray(payload.data)?payload.data:[];render()}catch(error){clearTimeout(timeout);message.textContent=error.name==="AbortError"?"Unable to load matches: request timed out":`Unable to load matches: ${error.message}`;container.innerHTML="";empty.classList.remove("hidden");empty.querySelector("h2").textContent="Unable to load matches";empty.querySelector("p").textContent="Check the GitHub Actions data update and try Refresh."}}
function normalize(v){return String(v??"").toLowerCase()}function matchText(m){return normalize(JSON.stringify(m))}function statusOf(m){const v=normalize(m.status||m.state||m.matchStatus);if(v.includes("live")||v.includes("progress")||v.includes("in play"))return"live";if(v.includes("complete")||v.includes("result")||v.includes("finish"))return"completed";return"upcoming"}function teamName(t){return typeof t==="string"?t:t?.name||t?.teamName||t?.short_name||t?.shortName||"Team"}function teamCountry(t){return typeof t==="object"?t?.country||t?.countryName||t?.code||"":""}const flags={india:"🇮🇳",australia:"🇦🇺",england:"🏴",pakistan:"🇵🇰",south:"🇿🇦",bangladesh:"🇧🇩",new:"🇳🇿",zealand:"🇳🇿",sri:"🇱🇰",lanka:"🇱🇰",afghanistan:"🇦🇫",ireland:"🇮🇪",scotland:"🏴",zimbabwe:"🇿🇼",nepal:"🇳🇵",netherlands:"🇳🇱"};function teamFlag(t){const c=normalize(teamCountry(t)||teamName(t));const k=Object.keys(flags).find(x=>c.includes(x));return k?flags[k]:""}function teamLogo(t){return typeof t==="object"?t?.logo_url||t?.logo||t?.image||"":""}function scorePart(v){if(v==null||v==="")return"—";if(typeof v==="object")return v.runs!=null?`${v.runs}/${v.wickets??0}`:"—";return String(v)}function scoreText(m,s){const score=m.score;if(!score||typeof score!=="object")return score==null?"—":String(score);return scorePart(score[s])}function oversText(m,s){const source=m.overs||m.score?.overs||m.innings;if(typeof source==="object"&&source?.[s]!=null)return`${source[s]} Ov`;if(m[`${s}_overs`]!=null)return`${m[`${s}_overs`]} Ov`;return"—"}function runRate(m,s){const source=m.run_rate||m.runRate||m.score?.run_rate;if(typeof source==="object"&&source?.[s]!=null)return Number(source[s]).toFixed(2);if(typeof source==="number")return source.toFixed(2);return""}function formatDate(v){const d=new Date(v);return Number.isNaN(d.getTime())?String(v||""):d.toLocaleString([],{dateStyle:"medium",timeStyle:"short"})}function escapeHtml(v){return String(v??"").replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[c]))}function venueText(m){const v=m.venue||m.stadium||m.ground||{};if(typeof v==="string")return[v,m.city||m.country].filter(Boolean);return[v.name||"Venue TBC",v.city||v.location||v.country||""].filter(Boolean)}function situationText(m){return m.situation||m.match_situation||m.description||""}function logoHtml(t){const logo=teamLogo(t),flag=teamFlag(t);return`<span class="team-mark">${logo?`<img src="${escapeHtml(logo)}" alt="">`:`<span class="team-initial">${escapeHtml(teamName(t).charAt(0))}</span>`}</span>${flag?`<span class="flag">${flag}</span>`:""}`}
function render(){const filtered=state.matches.filter(m=>{const s=statusOf(m),c=m.league||m.seriesName||m.series||m.competition||m.tournament||"Other";return(state.filter==="all"||s===state.filter)&&(state.competition==="all"||c===state.competition)&&(!state.query||matchText(m).includes(normalize(state.query)))}).sort((a,b)=>new Date(b.kickoff_utc||b.date||0)-new Date(a.kickoff_utc||a.date||0));$("#message").textContent=`${filtered.length} ${state.filter==="completed"?"results":"matches"}`;const container=$("#matches"),empty=$("#empty");if(filtered.length){container.innerHTML=filtered.map(renderMatch).join("");empty.classList.add("hidden")}else{container.innerHTML="";empty.classList.remove("hidden");empty.querySelector("h2").textContent="No matches found";empty.querySelector("p").textContent="Try another search or filter."}const competitions=[...new Set(state.matches.map(m=>m.league||m.seriesName||m.series||m.competition||m.tournament).filter(Boolean))].sort();const select=$("#series");if(select){const current=state.competition;select.innerHTML='<option value="all">All competitions</option>'+competitions.map(c=>`<option value="${escapeHtml(c)}">${escapeHtml(c)}</option>`).join("");select.value=competitions.includes(current)?current:"all"}}
function renderMatch(m){const home=m.home||m.homeTeam||m.team1||m.teamA,away=m.away||m.awayTeam||m.team2||m.teamB,c=m.league||m.seriesName||m.series||m.competition||m.tournament||"Cricket",date=m.kickoff_utc||m.date||m.startTime||m.startDate,s=statusOf(m),label=s==="live"?"LIVE":s==="completed"?"COMPLETED":"UPCOMING",v=venueText(m),hs=scoreText(m,"home"),as=scoreText(m,"away"),ho=oversText(m,"home"),ao=oversText(m,"away"),hr=runRate(m,"home"),ar=runRate(m,"away"),situation=situationText(m);return`<article class="match-card ${s==="live"?"is-live":""}" tabindex="0" role="button"><div class="match-head"><div><span class="tournament">${escapeHtml(c)}</span><span class="match-date">${date?escapeHtml(formatDate(date)):"Date TBC"}</span></div><span class="status-badge ${s}"><i></i>${label}</span></div><div class="match-grid"><div class="teams-cell"><div class="team-line"><span class="team-identity">${logoHtml(home)}<strong>${escapeHtml(teamName(home))}</strong></span><span class="batting-dot ${s==="live"?"on":""}"></span></div><div class="team-line"><span class="team-identity">${logoHtml(away)}<strong>${escapeHtml(teamName(away))}</strong></span></div></div><div class="score-cell"><div class="score-main"><strong>${escapeHtml(hs)}</strong><span>·</span><strong>${escapeHtml(as)}</strong></div><div class="overs">${escapeHtml(ho)}${hr?` · RR ${escapeHtml(hr)}`:""}</div><div class="away-overs">${escapeHtml(ao)}${ar?` · RR ${escapeHtml(ar)}`:""}</div></div><div class="status-cell"><span class="status-badge ${s}"><i></i>${label}</span>${situation?`<div class="situation">${escapeHtml(situation)}</div>`:""}</div><div class="venue-cell"><strong>${escapeHtml(v[0]||"Venue TBC")}</strong><span>${escapeHtml(v[1]||"")}</span></div><div class="action-cell"><button class="scorecard-btn" type="button">View Scorecard <span>→</span></button></div></div>${situation?`<div class="mobile-situation">${escapeHtml(situation)}</div>`:""}</article>`}
$("#refresh")?.addEventListener("click",load);$("#search")?.addEventListener("input",e=>{state.query=e.target.value;render()});$("#series")?.addEventListener("change",e=>{state.competition=e.target.value;render()});document.querySelectorAll("[data-filter]").forEach(b=>b.addEventListener("click",()=>{state.filter=b.dataset.filter;document.querySelectorAll("[data-filter]").forEach(x=>x.classList.toggle("active",x===b));render()}));load();
