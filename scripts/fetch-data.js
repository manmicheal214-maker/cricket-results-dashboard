const fs=require("fs"),path=require("path");
const BASE_URL="https://api.bigballsdata.com/v1/cricket",STORED_URL="https://api.bigballsdata.com/v1/stored/matches";
const extractMatches=p=>{const c=[p?.data,p?.data?.matches,p?.data?.results,p?.data?.items,p?.matches,p?.results,p?.items];return c.find(Array.isArray)||[]};
const text=v=>String(v??"").trim(),teamName=t=>typeof t==="string"?text(t):text(t?.name||t?.teamName||t?.short_name||t?.shortName),home=m=>m?.home||m?.homeTeam||m?.team1||m?.teamA,away=m=>m?.away||m?.awayTeam||m?.team2||m?.teamB,competition=m=>text(m?.league||m?.seriesName||m?.series||m?.competition||m?.tournament||"Other"),kickoff=m=>m?.kickoff_utc||m?.date||m?.startTime||m?.startDate||"";
const normalize=v=>text(v).toLowerCase().replace(/\s+/g," ");
const STATUS={live:"live",in_progress:"live",progress:"live","in play":"live",scheduled:"upcoming",pre:"upcoming",upcoming:"upcoming",finished:"completed",finish:"completed",complete:"completed",completed:"completed",result:"completed",results:"completed",final:"completed",post:"completed",abandoned:"completed","no result":"completed"};
function statusOf(m){const s=normalize(m?.status||m?.state||m?.matchStatus);return STATUS[s]||(s.includes("live")||s.includes("progress")||s.includes("in play")?"live":s.includes("complete")||s.includes("result")||s.includes("finish")||s.includes("final")?"completed":"upcoming")}
function fixtureKey(m){const d=kickoff(m),x=new Date(d),day=d?(Number.isNaN(x.getTime())?normalize(d).slice(0,10):x.toISOString().slice(0,10)):"";return`${normalize(teamName(home(m)))}|${normalize(teamName(away(m)))}|${day}|${normalize(competition(m))}`}
function quality(m){return(statusOf(m)==="live"?300:statusOf(m)==="completed"?200:100)+(m.score||m.scores||m.linescore?10:0)+Object.keys(m||{}).length}
function dedupe(a){const out=new Map;for(const m of a){const k=fixtureKey(m);if(!k||k.startsWith("|||"))continue;if(!out.has(k)||quality(m)>quality(out.get(k)))out.set(k,m)}return[...out.values()]}
async function apiRequest(base,p,key){const r=await fetch(`${base}${p}`,{headers:{Accept:"application/json",Authorization:`Bearer ${key}`}}),t=await r.text();let j;try{j=t?JSON.parse(t):{}}catch{throw Error(`API returned non-JSON (${r.status})`)}if(!r.ok)throw Error(String(j?.error?.message||j?.error||j?.message||`API returned ${r.status}`));return j}
function unwrap(p){return p?.data??p}function inningsCount(d){const r=d?.data??d;return Array.isArray(r?.innings)?r.innings.length:0}
async function fetchScorecards(matches,key){const dir=path.join(__dirname,"..","public","data","scorecards");fs.mkdirSync(dir,{recursive:true});const index={},candidates=matches.filter(m=>statusOf(m)==="live"||statusOf(m)==="completed").slice(0,20);let cursor=0;async function worker(){while(cursor<candidates.length){const m=candidates[cursor++],id=m?.match_id||m?.id||m?.matchId||m?.key;if(!id)continue;try{const data=unwrap(await apiRequest(BASE_URL,`/matches/${encodeURIComponent(String(id))}/scorecard`,key)),count=inningsCount(data),updatedAt=new Date().toISOString();fs.writeFileSync(path.join(dir,`${encodeURIComponent(String(id))}.json`),JSON.stringify({ok:count>0,id,data,inningsCount:count,updatedAt},null,2)+"\n");index[id]={available:count>0,inningsCount:count,updatedAt}}catch(e){index[id]={available:false,inningsCount:0,error:e.message,updatedAt:new Date().toISOString()}}}}await Promise.all([worker(),worker(),worker()]);fs.writeFileSync(path.join(dir,"index.json"),JSON.stringify({updatedAt:new Date().toISOString(),matches:index},null,2)+"\n")}
async function main(){const key=process.env.BBS_API_KEY;if(!key)throw Error("BBS_API_KEY is required");const results=await Promise.allSettled([apiRequest(BASE_URL,"/matches?limit=200",key),apiRequest(BASE_URL,"/matches?status=live&limit=200",key),apiRequest(STORED_URL,"?sport=cricket&status=finished&limit=200",key)]);const groups=results.filter(r=>r.status==="fulfilled").map(r=>extractMatches(r.value));const matches=dedupe(groups.flat());if(!matches.length){const errors=results.filter(r=>r.status==="rejected").map(r=>r.reason?.message).filter(Boolean);throw Error(errors.join("; ")||"Cricket API returned zero matches; refusing to publish an empty dashboard snapshot")}
const output={ok:true,updatedAt:new Date().toISOString(),sourceCounts:results.map((r,i)=>({source:["current","live","stored-finished"][i],ok:r.status==="fulfilled",count:r.status==="fulfilled"?extractMatches(r.value).length:0})),count:matches.length,data:matches},out=path.join(__dirname,"..","public","data","matches.json");fs.mkdirSync(path.dirname(out),{recursive:true});fs.writeFileSync(out,JSON.stringify(output,null,2)+"\n");if(process.env.FETCH_SCORECARDS!=="false")await fetchScorecards(matches,key);console.log(`Published ${matches.length} unique cricket matches`)}main().catch(e=>{console.error(e);process.exit(1)})
