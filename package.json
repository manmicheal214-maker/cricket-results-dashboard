{
  "name": "cricket-results-dashboard",
  "version": "2.0.0",
  "private": true,
  "description": "Google-style cricket results dashboard with full scorecards",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check src/cricket-api.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^5.1.0"
  }
}
